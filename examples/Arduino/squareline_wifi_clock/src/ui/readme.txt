SquareLine Studio generated UI files are exported into this directory.

Edit the UI in the SquareLine project and re-export it instead of manually editing the
generated screens, images, fonts, and helper files here. Re-exporting can overwrite local
changes. The Arduino sketch uses the generated `ui.h` entry point and the local `libraries/ui`
wrapper.
